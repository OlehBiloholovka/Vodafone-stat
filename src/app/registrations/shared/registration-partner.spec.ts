import { RegistrationPartner } from './registration-partner';

describe('RegistrationPartner', () => {
  it('should create an instance', () => {
    expect(new RegistrationPartner()).toBeTruthy();
  });
});
