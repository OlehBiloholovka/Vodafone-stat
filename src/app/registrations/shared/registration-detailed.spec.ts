import { RegistrationDetailed } from './registration-detailed';

describe('Registration', () => {
  it('should create an instance', () => {
    expect(new RegistrationDetailed()).toBeTruthy();
  });
});
